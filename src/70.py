def add_numbers(x: int, y: int) -> int:
    """
    Adds two integers.
    
    Args:
        x (int): The first number.
        y (int): The second number.
        
    Returns:
        int: The sum of x and y.
    """
    return x + y
